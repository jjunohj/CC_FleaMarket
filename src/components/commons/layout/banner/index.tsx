import styled from "@emotion/styled";

const Wrapper = styled.div`
  width: 120rem;
  height: 25rem;
  background-color: gray;
`;

export default function LayoutBanner() {
  return <Wrapper>배너입니다.</Wrapper>;
}
